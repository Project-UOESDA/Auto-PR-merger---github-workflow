# workflow name
name: test workflow
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

# jobs
jobs:
  build:
    name: Building test file
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: finalizing the test
        run: Completed, Hello,World!
